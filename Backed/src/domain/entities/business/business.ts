import { User } from "../user/user"

export class Business {
    id: string
    name: string
    address: string
    category: string
    email: string
    image: string
    phone: string
    user: User
    
    constructor(
    ){}
    }
